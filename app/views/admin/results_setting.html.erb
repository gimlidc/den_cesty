<%#
# @author gimli
# @version Mar 15, 2012
%>
<% if @walkers.nil? %>
  No record found
<% else %>
  <h2>Výsledky <%= @set_dc %>. dne cesty</h2>
  <%= form_tag('results_setting') do -%>
	<%=  select_tag("id", dc_select.html_safe, :include_blank => true) %>
	<%= submit_tag "vybrat" %>
  <%- end %>

  <%= form_tag("results_update") %>
  <%= hidden_field_tag :dc_id, @set_dc %>
  <table>
	<tbody>
	  <tr>
		<th>jmeno</th>
		<th>příjmení</th>
		<th>rok</th>
		<th>výkon</th>
	  </tr>
	  <% odd = true %>
	  <% @walkers.each do |walker| %>
		<%= walker.dc_id %>
		<% odd = !odd %>
	  <tr class="<%= if odd
  "odd-table-line"
	  else
		"even-table-line"
	  end
%>">
		  <td><%= walker.name %></td>
		  <td><%= walker.surname %></td>
		  <td><%= walker.year %></td>
		  <td><%= number_field_tag walker.id, walker.distance, :size => 4, :step => 0.5 %></td>
		</tr>
	  <%  end %>
	  <tr> <!-- last line contains only submit button //-->
		<td></td><td></td><td></td><td><%= submit_tag "save" %></td>
	  </tr>
	</tbody>
  </table>
<% end %>