<h2><%= I18n.t('edit registration', :user => @walker.vokativ, :dc_id => $dc.id) %></h2>
<br />
<% if !isLimit || !@registration.canceled  %>
	<%= render  'form' %>
<% else%>
	<%= render 'fullRaceNotification' %>
<% end%>
